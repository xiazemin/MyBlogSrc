I"�<p>输出pod中一个容器的日志。如果pod只包含一个容器则可以省略容器名。</p>

<p>kubectl logs [-f] [-p] POD [-c CONTAINER]
<!-- more --></p>
<h1 id="返回仅包含一个容器的pod-nginx的日志快照">返回仅包含一个容器的pod nginx的日志快照</h1>
<p>$ kubectl logs nginx</p>

<h1 id="返回pod-ruby中已经停止的容器web-1的日志快照">返回pod ruby中已经停止的容器web-1的日志快照</h1>
<p>$ kubectl logs -p -c ruby web-1</p>

<h1 id="持续输出pod-ruby中的容器web-1的日志">持续输出pod ruby中的容器web-1的日志</h1>
<p>$ kubectl logs -f -c ruby web-1</p>

<h1 id="仅输出pod-nginx中最近的20条日志">仅输出pod nginx中最近的20条日志</h1>
<p>$ kubectl logs –tail=20 nginx</p>

<h1 id="输出pod-nginx中最近一小时内产生的所有日志">输出pod nginx中最近一小时内产生的所有日志</h1>
<p>$ kubectl logs –since=1h nginx</p>

<p>https://www.kubernetes.org.cn/doc-64</p>
:ET