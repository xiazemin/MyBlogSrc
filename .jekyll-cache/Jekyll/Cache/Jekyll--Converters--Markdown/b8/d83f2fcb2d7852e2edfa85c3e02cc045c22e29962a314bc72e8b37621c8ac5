I"g<p>curl -X POST “localhost:9200/twitter/_delete_by_query” -H ‘Content-Type: application/json’ -d’
{
  “query”: { 
    “match”: {
      “name”: “测试删除”
    }
  }
}
‘
<!-- more -->
查询必须是有效的键值对，query是键，这和Search API是同样的方式。在search api中q参数和上面效果是一样的。</p>
:ET