I"�<p>题目：请问下面的程序一共输出多少个“-”？
`#include <stdio.h>
#include &lt;sys/types.h&gt;
#include <unistd.h></unistd.h></stdio.h></p>

<p>int main(void)
{
   int i;
   for(i=0; i&lt;2; i++){
      fork();
      printf(“-“);
   }</p>

<p>wait(NULL);
   wait(NULL);</p>

<p>return 0;
}`
<!-- more -->
如果你对fork()的机制比较熟悉的话，这个题并不难，输出应该是6个“-”，但是，实际上这个程序会很tricky地输出8个“-”。</p>
:ET