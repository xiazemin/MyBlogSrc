I")<p>使用 Go 语言编写的网络协议栈。
https://github.com/google/netstack</p>

<p>go install github.com/google/netstack/tcpip/sample/
<!-- more -->
tun_tcp_echo
创建一个TUN设备：</p>

<p>[sudo] ip tuntap add user <username> mode tun <device-name>
[sudo] ip link set <device-name> up
[sudo] ip addr add <ipv4-address>/<mask-length> dev <device-name>
运行：</device-name></mask-length></ipv4-address></device-name></device-name></username></p>

<p>tun_tcp_echo <device-name> <ipv4-address> <port></port></ipv4-address></device-name></p>
:ET