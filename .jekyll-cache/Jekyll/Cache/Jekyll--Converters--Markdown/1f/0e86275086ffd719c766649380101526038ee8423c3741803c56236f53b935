I"�<p>当启动一个C实现的thread时，C标准库会负责分配一块内存作为这个线程的栈。标准库分配这块内存，告诉内核它的位置并让内核处理这个线程 的执行。
在linux系统中，可通过 ulimit -s查看系统栈大小（8M）。
ulimit -s 10240可修改栈大小为10M。
这里最大的一个问题是，分配大数组，或者循环递归函数时，默认的栈空间不够用，会导致Segmentation fault错误。
解决方法有两个：</p>
:ET