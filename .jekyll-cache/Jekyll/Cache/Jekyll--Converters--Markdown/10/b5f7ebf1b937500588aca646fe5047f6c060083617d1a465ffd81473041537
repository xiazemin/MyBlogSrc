I"�<p>chan用于high-level （高端的编程，比如说，两个程序（运行在同一台电脑，或者不同电脑上），或者两个线程（thread）之间的sync）, 而锁用于low-level（低端编程，单个变量的sync). 
<!-- more -->
如果把线程安全定义为允许多个goroutine同时去读写，那么golang 的channel 是线程安全的。不需要在并发读写同一个channe时加锁。
channel用于goroutine之间的通信</p>
:ET